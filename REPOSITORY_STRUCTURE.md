# 📦 هيكل المستودع - Repository Structure

## ✅ ما يتم رفعه على الفرع الرئيسي (main)

### مجلد App:
```
app/
├── src/              ✅ كود المصدر
├── public/           ✅ الملفات الثابتة
├── package.json      ✅ الاعتمادات
├── vite.config.ts    ✅ إعدادات Vite
├── capacitor.config.ts  ✅ إعدادات Capacitor
└── .env.example      ✅ مثال لمتغيرات البيئة
```

### مجلد Server:
```
server/
├── controllers/      ✅ الكنترولرات
├── models/           ✅ النماذج
├── routes/           ✅ المسارات
├── middlewares/      ✅ الـ middlewares
├── package.json      ✅ الاعتمادات
└── .env.example      ✅ مثال لمتغيرات البيئة
```

### GitHub Actions:
```
.github/
└── workflows/
    ├── deploy.yml    ✅ ملف النشر التلقائي
    └── README.md     ✅ تعليمات الإعداد
```

---

## ❌ ما لا يتم رفعه (محمي بواسطة .gitignore)

### ملفات البناء:
```
app/dist/             ❌ بناء الويب (يُبنى بواسطة CI/CD)
app/build/            ❌ ملفات بناء إضافية
app/ios/              ❌ مشروع iOS (يُبنى بواسطة CI/CD)
app/android/          ❌ مشروع Android (يُبنى بواسطة CI/CD)
```

### ملفات حساسة:
```
.env                  ❌ متغيرات البيئة الفعلية
.env.local            ❌ متغيرات البيئة المحلية
server/public/images/* ❌ الصور المرفوعة (عدا default)
```

### ملفات النظام:
```
node_modules/         ❌ المكتبات (تُثبت بواسطة npm)
.DS_Store             ❌ ملفات macOS
*.log                 ❌ ملفات السجلات
```

---

## 🔄 سير العمل التلقائي

عند الـ Push للفرع الرئيسي، GitHub Actions يقوم بـ:

1. **بناء الويب** → ينشر في فرع `web` + GitHub Pages
2. **بناء iOS** → ينشر في فرع `ios`
3. **بناء Android** → ينشر في فرع `android`
4. **نسخ السيرفر** → ينشر في فرع `server`

---

## 📝 ملاحظات مهمة

### للتطوير المحلي:
1. انسخ `.env.example` إلى `.env.local` في مجلد `app/`
2. انسخ `.env.example` إلى `.env` في مجلد `server/`
3. عدّل المتغيرات حسب بيئتك المحلية

### عند الـ Commit:
- ✅ ارفع فقط كود المصدر
- ✅ تأكد من `.env.example` محدث
- ❌ لا ترفع ملفات `.env` الفعلية
- ❌ لا ترفع مجلدات البناء (dist, ios, android)
- ❌ لا ترفع `node_modules/`

### الفروع المنفصلة (Orphan Branches):
- `server` - يحتوي فقط على كود السيرفر
- `web` - يحتوي فقط على ملفات الويب المبنية
- `ios` - يحتوي فقط على مشروع iOS
- `android` - يحتوي فقط على مشروع Android

هذه الفروع **لا تُعدل يدوياً** - يتم تحديثها تلقائياً بواسطة GitHub Actions!

---

## 🔒 الأمان

تم حماية الملفات الحساسة التالية:
- ✅ `.env` - متغيرات البيئة
- ✅ `server/public/images/*` - الصور المرفوعة من المستخدمين
- ✅ `node_modules/` - المكتبات
- ✅ ملفات السجلات والأخطاء
