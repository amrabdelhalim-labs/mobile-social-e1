# Server Configuration
# ===================
PORT=3000
NODE_ENV=development

# Database Configuration (local development)
# Option 1: Individual parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=myrecipes
DB_USER=postgres
DB_PASS=123456

# Option 2: Heroku PostgreSQL URL (set this on Heroku)
# DATABASE_URL=postgresql://user:password@host:port/database
# Leave commented for local development

# JWT Configuration
JWT=your_jwt_secret_key_here

# Instructions (تعليمات):
# =====================
# Local Development:
#   1. Copy this file to .env
#   2. Fill in your local database credentials
#   3. Run: npm run dev
#
# Heroku Production:
#   1. Set environment variables using Heroku CLI or dashboard
#   2. heroku config:set DATABASE_URL='postgresql://...'
#   3. heroku config:set JWT='your_secret_key'
#   4. Run: npm start